#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) 
{
	char a;
	printf("please put in the character");
	scanf("%c",&a);
	printf("the ASCII number is %d",a);
	return 0;
}
